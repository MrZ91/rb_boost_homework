.container-fluid
 .row
  .col-md-8.col-md-offset-2
   %h4
    Status: #{@advancement.state}
    = link_to user_course_lesson_advancement_approve_path(@course, @lesson, @advancement), method: :post do
     = button_tag 'Approve', class: 'btn btn-success pull-right', style:('display: none' if @advancement.approved? || @advancement.approved?)
    = link_to user_course_lesson_advancement_reject_path(@course, @lesson, @advancement), method: :post do
     = button_tag 'Reject', class: 'btn btn-danger pull-right', style:('display: none' if @advancement.approved? || @advancement.approved?)
   %br/
   #{@advancement.user.first_name} #{@advancement.user.last_name} advancement to
   = link_to 'lesson:', user_course_lesson_advancements_path(@course, @lesson)
   %br/
   #{@advancement.version}

